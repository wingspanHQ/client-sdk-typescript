/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import { z } from "zod";

export type CardTokenResponse = {
    cardId: string;
    token: string;
};

/** @internal */
export namespace CardTokenResponse$ {
    export type Inbound = {
        cardId: string;
        token: string;
    };

    export const inboundSchema: z.ZodType<CardTokenResponse, z.ZodTypeDef, Inbound> = z
        .object({
            cardId: z.string(),
            token: z.string(),
        })
        .transform((v) => {
            return {
                cardId: v.cardId,
                token: v.token,
            };
        });

    export type Outbound = {
        cardId: string;
        token: string;
    };

    export const outboundSchema: z.ZodType<Outbound, z.ZodTypeDef, CardTokenResponse> = z
        .object({
            cardId: z.string(),
            token: z.string(),
        })
        .transform((v) => {
            return {
                cardId: v.cardId,
                token: v.token,
            };
        });
}
