/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import { MemberProfile, MemberProfile$ } from "./memberprofile";
import { NewUser, NewUser$ } from "./newuser";
import { ServiceStatus, ServiceStatus$ } from "./servicestatus";
import { UserRoles, UserRoles$ } from "./userroles";
import { z } from "zod";

export type Member = {
    advocateId?: string | null | undefined;
    createdAt: string;
    enabledServices: Record<string, boolean>;
    eventActors: Record<string, string>;
    labels: Record<string, string>;
    memberId: string;
    profile: MemberProfile;
    serviceStatus: ServiceStatus;
    updatedAt: string;
    user: NewUser;
    userRoles: UserRoles;
};

/** @internal */
export namespace Member$ {
    export type Inbound = {
        advocateId?: string | null | undefined;
        createdAt: string;
        enabledServices: Record<string, boolean>;
        eventActors: Record<string, string>;
        labels: Record<string, string>;
        memberId: string;
        profile: MemberProfile$.Inbound;
        serviceStatus: ServiceStatus$.Inbound;
        updatedAt: string;
        user: NewUser$.Inbound;
        userRoles: UserRoles$.Inbound;
    };

    export const inboundSchema: z.ZodType<Member, z.ZodTypeDef, Inbound> = z
        .object({
            advocateId: z.nullable(z.string()).optional(),
            createdAt: z.string(),
            enabledServices: z.record(z.boolean()),
            eventActors: z.record(z.string()),
            labels: z.record(z.string()),
            memberId: z.string(),
            profile: MemberProfile$.inboundSchema,
            serviceStatus: ServiceStatus$.inboundSchema,
            updatedAt: z.string(),
            user: NewUser$.inboundSchema,
            userRoles: UserRoles$.inboundSchema,
        })
        .transform((v) => {
            return {
                ...(v.advocateId === undefined ? null : { advocateId: v.advocateId }),
                createdAt: v.createdAt,
                enabledServices: v.enabledServices,
                eventActors: v.eventActors,
                labels: v.labels,
                memberId: v.memberId,
                profile: v.profile,
                serviceStatus: v.serviceStatus,
                updatedAt: v.updatedAt,
                user: v.user,
                userRoles: v.userRoles,
            };
        });

    export type Outbound = {
        advocateId?: string | null | undefined;
        createdAt: string;
        enabledServices: Record<string, boolean>;
        eventActors: Record<string, string>;
        labels: Record<string, string>;
        memberId: string;
        profile: MemberProfile$.Outbound;
        serviceStatus: ServiceStatus$.Outbound;
        updatedAt: string;
        user: NewUser$.Outbound;
        userRoles: UserRoles$.Outbound;
    };

    export const outboundSchema: z.ZodType<Outbound, z.ZodTypeDef, Member> = z
        .object({
            advocateId: z.nullable(z.string()).optional(),
            createdAt: z.string(),
            enabledServices: z.record(z.boolean()),
            eventActors: z.record(z.string()),
            labels: z.record(z.string()),
            memberId: z.string(),
            profile: MemberProfile$.outboundSchema,
            serviceStatus: ServiceStatus$.outboundSchema,
            updatedAt: z.string(),
            user: NewUser$.outboundSchema,
            userRoles: UserRoles$.outboundSchema,
        })
        .transform((v) => {
            return {
                ...(v.advocateId === undefined ? null : { advocateId: v.advocateId }),
                createdAt: v.createdAt,
                enabledServices: v.enabledServices,
                eventActors: v.eventActors,
                labels: v.labels,
                memberId: v.memberId,
                profile: v.profile,
                serviceStatus: v.serviceStatus,
                updatedAt: v.updatedAt,
                user: v.user,
                userRoles: v.userRoles,
            };
        });
}
