/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import { z } from "zod";

export type MccResponse = {
    code: string;
    title: string;
};

/** @internal */
export namespace MccResponse$ {
    export type Inbound = {
        code: string;
        title: string;
    };

    export const inboundSchema: z.ZodType<MccResponse, z.ZodTypeDef, Inbound> = z
        .object({
            code: z.string(),
            title: z.string(),
        })
        .transform((v) => {
            return {
                code: v.code,
                title: v.title,
            };
        });

    export type Outbound = {
        code: string;
        title: string;
    };

    export const outboundSchema: z.ZodType<Outbound, z.ZodTypeDef, MccResponse> = z
        .object({
            code: z.string(),
            title: z.string(),
        })
        .transform((v) => {
            return {
                code: v.code,
                title: v.title,
            };
        });
}
