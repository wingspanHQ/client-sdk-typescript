/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import { z } from "zod";

export type InvoiceAttachmentFile = {
    fileName: string;
    fileUrl: string;
};

/** @internal */
export namespace InvoiceAttachmentFile$ {
    export type Inbound = {
        fileName: string;
        fileUrl: string;
    };

    export const inboundSchema: z.ZodType<InvoiceAttachmentFile, z.ZodTypeDef, Inbound> = z
        .object({
            fileName: z.string(),
            fileUrl: z.string(),
        })
        .transform((v) => {
            return {
                fileName: v.fileName,
                fileUrl: v.fileUrl,
            };
        });

    export type Outbound = {
        fileName: string;
        fileUrl: string;
    };

    export const outboundSchema: z.ZodType<Outbound, z.ZodTypeDef, InvoiceAttachmentFile> = z
        .object({
            fileName: z.string(),
            fileUrl: z.string(),
        })
        .transform((v) => {
            return {
                fileName: v.fileName,
                fileUrl: v.fileUrl,
            };
        });
}
