/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import { z } from "zod";

export type BankStatement = {
    accountId: string;
    date: string;
    id: string;
    name: string;
};

/** @internal */
export namespace BankStatement$ {
    export type Inbound = {
        accountId: string;
        date: string;
        id: string;
        name: string;
    };

    export const inboundSchema: z.ZodType<BankStatement, z.ZodTypeDef, Inbound> = z
        .object({
            accountId: z.string(),
            date: z.string(),
            id: z.string(),
            name: z.string(),
        })
        .transform((v) => {
            return {
                accountId: v.accountId,
                date: v.date,
                id: v.id,
                name: v.name,
            };
        });

    export type Outbound = {
        accountId: string;
        date: string;
        id: string;
        name: string;
    };

    export const outboundSchema: z.ZodType<Outbound, z.ZodTypeDef, BankStatement> = z
        .object({
            accountId: z.string(),
            date: z.string(),
            id: z.string(),
            name: z.string(),
        })
        .transform((v) => {
            return {
                accountId: v.accountId,
                date: v.date,
                id: v.id,
                name: v.name,
            };
        });
}
